templates:
  - httproute.yaml
tests:
  - it: should work
    set:
      image.tag: latest
    asserts:
      - isKind:
          of: HTTPRoute
      # or you can snapshot the whole manifest
      - matchSnapshot: {}
