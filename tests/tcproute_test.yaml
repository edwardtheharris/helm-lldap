templates:
  - tcproute.yaml
tests:
  - it: should work
    set:
      image.tag: latest
    asserts:
      - isKind:
          of: TCPRoute
      # or you can snapshot the whole manifest
      - matchSnapshot: {}
