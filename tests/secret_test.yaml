templates:
  - secret.yaml
values:
  - ../thw.values.yaml
tests:
  - it: should work
    set:
      image.tag: latest
    asserts:
      - isKind:
          of: Secret
      # or you can snapshot the whole manifest
      - matchSnapshot: {}
