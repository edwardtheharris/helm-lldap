templates:
  - pvc.yaml
tests:
  - it: should work
    set:
      image.tag: latest
    asserts:
      - isKind:
          of: PersistentVolumeClaim
      # or you can snapshot the whole manifest
      - matchSnapshot: {}
